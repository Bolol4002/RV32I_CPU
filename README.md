# RV32I_CPU

## Modules in a single pip cpu
- Regfile.sv
    It has wen when high and if the data is not x0 the data is stored in given adress or else asyncronsouly it reads from the address given and pass it to the output terminals.
